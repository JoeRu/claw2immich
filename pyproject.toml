[project]
name = "claw2immich"
version = "0.1.0"
description = "MCP Server for [immich-app](https://immich.app/)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]
