services:
  claw2immich:
    build: .
    ports:
      - "${MCP_PORT:-8765}:8765"
    environment:
      IMMICH_BASE_URL: ${IMMICH_BASE_URL:-http://localhost:2283}
      IMMICH_API_KEY: ${IMMICH_API_KEY:-}
      IMMICH_API_TOKEN: ${IMMICH_API_TOKEN:-}
    stdin_open: true
    tty: true
